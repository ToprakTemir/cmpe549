# Phase 1-3: DNABERT-2 / HyenaDNA fine-tuning + Integrated Gradients
torch>=2.1
transformers==4.41.2
tokenizers>=0.19,<0.20
datasets>=2.14,<3.0
peft>=0.10,<0.13
accelerate>=0.28,<0.34
huggingface_hub>=0.20,<0.24

evaluate>=0.4
scikit-learn>=1.3

# Phase 3: Integrated Gradients
captum>=0.7
tqdm>=4.66

einops>=0.7         # required by DNABERT-2 + HyenaDNA custom modeling code
triton>=2.0         # used by DNABERT-2 attention (we disable via runtime patch)
numpy<2.0           # transformers 4.41 isn't fully NumPy-2 compatible