# Phase 1: DNABERT-2 fine-tuning with LoRA on GUE TFBS
# torch is intentionally unpinned — Colab provides a working CUDA build; locally
# install whichever wheel matches your CUDA / CPU.

torch>=2.1
transformers==4.41.2
tokenizers>=0.19,<0.20
datasets>=2.14,<3.0
peft>=0.10,<0.13
accelerate>=0.28,<0.34
huggingface_hub>=0.20,<0.24

evaluate>=0.4
scikit-learn>=1.3

einops>=0.7         # required by DNABERT-2 custom modeling code
#triton>=2.0         # used by DNABERT-2 attention; harmless if unused on CPU
numpy<2.0           # transformers 4.41 isn't fully NumPy-2 compatible
